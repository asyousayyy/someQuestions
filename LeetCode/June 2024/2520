int countDigits(int num) {
        
        int cnt = 0;
        string s = to_string(num);
        for(int i=0; i<s.size(); i++){
            int digit = s[i]-'0';
            if(num%digit==0) cnt++;
        }
        return cnt;
    }
